// 두 정수 입력받아 비교하기2
// == : 두 개의 값이 같은 경우 참(true)을 나타내는 정수값 1
//      다른 경우 거짓(false)을 나타내는 정수값 0
#include <stdio.h>

int main()
{
	int a, b;
	scanf("%d %d", &a, &b);
	printf("%d", a == b);
	return 0;
}
